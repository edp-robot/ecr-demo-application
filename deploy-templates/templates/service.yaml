apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecr-demo-application.fullname" . }}
  labels:
    {{- include "ecr-demo-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecr-demo-application.selectorLabels" . | nindent 4 }}
